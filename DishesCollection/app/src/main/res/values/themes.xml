<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DishesCollection" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white_dark</item>
    </style>

    <style name="Theme.DishesCollection" parent="Base.Theme.DishesCollection" />

    <style name="NewTitleFont">
        <item name="android:fontFamily">@font/app_font</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TabTextFont" parent="TextAppearance.Design.Tab">
        <item name="fontFamily">@font/app_font</item>
        <item name="textAllCaps">false</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>